# T00 - Development tooling and test configuration

- **ID:** T00
- **Implementation:** VERIFIED
- **Review:** REVIEWED
- **Shipping:** COMMITTED
- **Dependencies:** None.
- **Index:** [T00 in the task index](../plan.md#t00--development-tooling-and-test-configuration)
- **Sources:** [Project instructions](../../AGENTS.md), [assignment](../assignment.md), and [technical decisions](../decisions.md).

## Goal

Complete only the missing development and test tooling so subsequent tasks can use the selected stack. Preserve working configuration and existing user content. This document contains the accepted plan and its implementation evidence.

## Inspection and existing validation

Inspected on 2026-09-09. Read the existing preparation status in the decisions document and the setup-validation results in the current conversation, then rechecked the repository rather than assuming configuration had changed.

| Area           | Current evidence                                                                                                                              | Remaining work                                                                                                                            |
| -------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Runtime        | Node.js 24.21.0 and npm 11.19.0 via `npm.cmd`.                                                                                                | Preserve the runtime; check new packages' engine and peer requirements before installing them during build.                               |
| React and Vite | React 19.2.8, Vite 8.2.2, React plugin 6.1.1; starter renders with local state and React StrictMode.                                          | Preserve the scaffold and React plugin. React StrictMode does not enable TypeScript strict checking.                                      |
| TypeScript     | Installed 6.0.3. Referenced app and Node configurations omit `strict`; existing no-unused and bundler settings are present.                   | Enable strict mode in both configurations while retaining existing settings.                                                              |
| CSS Modules    | Existing Vite setup and `vite/client` types provide the existing foundation; current components use global CSS.                               | Retain the foundation; no CSS migration or extra CSS tooling is needed in T00.                                                            |
| Test tooling   | No test files, test configuration, setup file, test scripts, or requested testing packages were found.                                        | Add the missing packages and configuration, without inventing functional tests for this setup task.                                       |
| ESLint         | Installed 10.10.0; TypeScript, Hooks, and Refresh rules exist. Current lint check passes.                                                     | Retain rules, integrate the installed Prettier conflict-disabling config, and account for test setup and generated output.                |
| Prettier       | Installed 3.9.6; explicit configuration, ignore file, and formatting scripts are absent. Prior preparation-document formatting checks passed. | Add minimal explicit configuration and scripts consistent with existing source style.                                                     |
| npm lockfile   | Version 3; manifest dependency declarations match its root package, and all installed direct versions match their lock entries.               | Preserve the lockfile; update only as needed for missing test dependencies and their required dependencies.                               |
| Git ignores    | `node_modules`, `dist`, `dist-ssr`, and `*.local` already exist. Coverage and general `.env` variants are not covered.                        | Add the missing patterns while preserving existing editor/log rules.                                                                      |
| Git state      | Project and repository root: `D:/Raksul/raksul-price-table`. Branch `master`, no commits; all project files are untracked.                    | Preserve all pre-existing files and distinguish task edits from the untracked baseline. No Git mutation belongs to this planning request. |

The task index exists and uniquely resolves T00. There are no prerequisite tasks or missing requirements blocking this plan.

## Acceptance criteria

1. TypeScript strict mode is enabled for application and tooling code without disabling existing checks or changing module settings unnecessarily.
2. Vitest, React Testing Library, user-event, jest-dom, and jsdom are available as development dependencies compatible with the retained runtime and toolchain.
3. Test configuration supports TypeScript/TSX component tests in jsdom, loads jest-dom matchers, and performs cleanup between tests. Setup and configuration participate in TypeScript checking. Automated tests do not depend on the live API.
4. Existing ESLint rules remain effective and lint passes. The already-installed `eslint-config-prettier` is used without disabling unrelated correctness rules.
5. Explicit Prettier configuration, generated/local-file exclusions, and usable write/check scripts exist. Formatting follows the existing source conventions; content and behavior are preserved.
6. Development, build, lint, one-shot tests, watch tests, formatting, and formatting checks have distinct usable npm scripts. Preserve the current preview script.
7. The manifest and lockfile remain consistent. Existing packages are not reinstalled or upgraded without a concrete compatibility reason.
8. Git ignores dependencies, build output, coverage, and local environment files while allowing a deliberately sanitized example environment file.
9. Applicable commands and their actual outcomes are recorded. If no tests exist at T00 completion, explicitly record the empty suite and test command's actual exit status. Do not add `passWithNoTests`, mask failures, or claim tests passed. T01 supplies the first functional verification of the runner.

## Scope and non-goals

Scope is the remaining configuration, necessary test dependencies, package scripts, lockfile changes, and ignore rules. Include minimal formatting-only corrections if the new formatting check exposes existing style differences; inspect and document their file scope first.

Do not implement the formatter, price table, API access, or other product features. Do not migrate the starter to CSS Modules, add a demonstration test solely to produce green output, introduce a coverage provider or threshold, add browser automation, CI, hooks, routing, a store, or a backend. Do not replace ESLint or Vite configuration wholesale, add redundant test configs, regenerate the lockfile from scratch, or perform a blanket dependency upgrade. README completion remains T07.

The planning phase edited only this task document and the T00 index entry. The build phase implemented the tooling scope recorded below without implementing product functionality, committing, or pushing.

## Implementation assumptions

These are task-level choices, separate from assignment requirements:

- The repeated `test, test` and `format, format` entries in the request mean separate one-shot/watch and write/check scripts. Use the names below unless later user context refines them.
- Extend the existing `vite.config.ts` with Vitest configuration using its typed configuration API, preserving the React plugin. Do not introduce a separate Vitest config without a concrete conflict.
- Prefer explicit Vitest imports in future tests and in setup over adding test globals to application types. Use a setup file at `src/test/setup.ts` for the Vitest-specific jest-dom entry point and explicit React Testing Library cleanup after each test.
- Use minimal Prettier options matching the existing TypeScript source's single quotes and no semicolons; retain defaults for options without a demonstrated need.
- Keep a sanitized `.env.example` eligible for version control, but do not create one when the application has no environment-variable requirement.

| Script       | Intended command       | Action                                    |
| ------------ | ---------------------- | ----------------------------------------- |
| dev          | `vite`                 | Preserve.                                 |
| build        | `tsc -b && vite build` | Preserve.                                 |
| lint         | `eslint .`             | Preserve.                                 |
| preview      | `vite preview`         | Preserve.                                 |
| test         | `vitest run`           | Add deterministic one-shot execution.     |
| test:watch   | `vitest --watch`       | Add explicit interactive watch execution. |
| format       | `prettier --write .`   | Add intentional formatting.               |
| format:check | `prettier --check .`   | Add non-writing verification.             |

## Relevant files

Paths in this section are relative to the repository root.

| File                                                   | Planned treatment                                                                    |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------ |
| `package.json`                                         | Add only missing testing development dependencies and four missing scripts.          |
| `package-lock.json`                                    | Let npm update it with the dependency addition; inspect unexpected churn.            |
| `tsconfig.app.json`, `tsconfig.node.json`              | Enable strict mode; retain existing options and verify test/setup/config inclusion.  |
| `tsconfig.json`                                        | Inspect reference coverage; change only if demonstrably necessary.                   |
| `vite.config.ts`                                       | Retain React integration and add jsdom/setup configuration.                          |
| `src/test/setup.ts`                                    | Future output for matchers and cleanup; not a functional test.                       |
| `eslint.config.js`                                     | Integrate installed Prettier configuration last and extend output ignores as needed. |
| `.prettierrc.json`, `.prettierignore`                  | Future outputs for minimal formatting choices and appropriate exclusions.            |
| `.gitignore`                                           | Add coverage and environment patterns; preserve existing rules.                      |
| `src/App.tsx`, `src/main.tsx`, existing CSS and assets | Inspect for compatibility; preserve behavior and styling. No product refactor.       |
| `docs/tasks/T00.md`, `docs/plan.md`                    | Record progress, evidence, and accurate status.                                      |

## Implementation steps

1. Reinspect the working tree and configuration against this baseline. Preserve any intervening user changes. Confirm that dependency-free prerequisites remain satisfied.
2. Check version compatibility for only the missing test packages: `vitest`, `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom`, and `jsdom`. Add compatible development versions through npm in one scoped update. If a required peer is absent, verify the package metadata and record its concrete need before adding it. Do not delete or rebuild the existing installation merely to install these additions.
3. Enable TypeScript strict mode in the app and Node projects. Preserve their existing targets, module modes, references, and checks. Resolve any actual errors minimally rather than weakening settings.
4. Extend the existing Vite configuration and create the setup file. Configure jsdom and setup loading, keep test globals explicit, and ensure cleanup does not depend on globals being enabled. Verify the setup and test types without adding a placeholder test.
5. Retain ESLint's current recommendations and React rules. Add its existing Prettier compatibility config at the end and ignore generated coverage alongside build output. Add environment-specific overrides only if the actual files require them; avoid introducing extra lint plugins.
6. Add minimal Prettier configuration and exclusions for dependency/build/coverage output and local environment material. Add the script variants above. Inspect formatting differences before applying only necessary formatting-only corrections; do not exclude maintained source or documentation just to pass the check.
7. Add missing Git ignore patterns such as `coverage/`, `.env`, and `.env.*` with a deliberate `!.env.example` exception. Retain the existing `*.local`, editor, and log rules.
8. Run the verification below, inspect configuration and lockfile changes for scope, and record actual evidence here. Set implementation to `VERIFIED` only when T00 criteria are satisfied under the documented empty-test exception; otherwise use `IN_PROGRESS`. Do not commit or push during task-build.

## Verification plan and meaningful cases

These checks defined the implementation verification. Actual results are recorded under Implementation evidence.

| Check                                                                        | Expected evidence                                                                                                                                                                                       |
| ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Manifest/lock inspection and `npm.cmd ls --depth=0`                          | Direct dependencies resolve consistently; no unaccounted invalid or missing peers.                                                                                                                      |
| Effective TypeScript configuration for both projects and `npm.cmd run build` | Strict mode is effective; TypeScript checks setup/config files; production build succeeds.                                                                                                              |
| `npm.cmd run lint`                                                           | Existing correctness rules remain enabled and lint succeeds.                                                                                                                                            |
| `npm.cmd run format:check`                                                   | Maintained files pass formatting and generated/local files are excluded appropriately.                                                                                                                  |
| `npm.cmd test`                                                               | If no tests exist, capture the no-tests result and actual nonzero exit instead of reporting success. Configuration/import errors remain genuine failures and must be distinguished from an empty suite. |
| `npm.cmd run test:watch`                                                     | Runner starts in watch mode without configuration errors; stop it after inspection. No functional pass is claimed for an empty suite.                                                                   |
| `npm.cmd run dev`                                                            | Existing starter starts successfully; stop the process afterward. Verify in a real browser if available, otherwise record that browser smoke verification remains.                                      |
| `git check-ignore` with representative paths                                 | Dependencies, dist, coverage, `.env`, `.env.development`, and `.env.local` are ignored; a sanitized `.env.example` is not. Do not read environment contents.                                            |
| Final task diff and file inventory                                           | Only necessary tooling/dependency changes and scoped formatting appear; no product code or placeholder tests were added.                                                                                |

T00 configures the component-test environment; it does not prove component interaction behavior with an empty suite. T01's formatter tests provide the first functional runner verification. T02's controlled component tests will exercise render, user interactions, DOM matchers, cleanup, and loading/error behavior. Do not claim these later checks are complete during T00.

## Risks and blockers

- The selected test package versions were checked against the installed Vite, React, TypeScript, and Node versions. Recheck compatibility before any future package upgrade.
- PowerShell blocks the ordinary npm script shim in the earlier inspection; `npm.cmd` works. Use it without changing the machine's execution policy.
- Sandbox process startup currently fails with a setup-refresh error. Read-only inspection succeeded through approved execution outside the sandbox. This environment limitation may also affect later edits and commands; do not change sandbox or machine configuration implicitly.
- Git inspection requires a command-scoped ownership exception in this environment. Preserve persistent Git configuration. No commits exist, so future task shipping must distinguish required scaffold files from unrelated untracked preparation work.
- Formatting defaults differ from current source style. Keep configuration aligned with existing style and inspect any residual changes rather than running an unexplained repository rewrite.
- Missing original assignment text and copyable mockup are documented in the assignment; neither blocks tooling work.
- No tests exist now. An empty suite is an explicit T00 limitation, not evidence of correct component setup or a passing test run.

## Progress and verification evidence

### Planning inspection - 2026-09-09

- Read the task index, instructions, assignment, decisions, current configuration, starter source, and README.
- `node --version`: passed, 24.21.0. `npm.cmd --version`: passed, 11.19.0.
- Node-based read-only manifest/lock/installed-package comparison: passed for all direct dependencies; lockfile version 3. No requested testing packages found in the lockfile.
- `node node_modules/eslint/bin/eslint.js .`: passed, exit 0, on the current starter configuration.
- Repository file inspection: no tests, test setup, test configuration, or detailed T00 document existed before this plan.
- Read-only Git status: `master`, no commits; existing files are untracked and must be preserved.
- Production build, dev server, test execution, and whole-project formatting checks were not run during planning. No tests passed or failed because no test suite was executed.
- Implementation has not started. The only new output is this planning document; the index implementation status changes to `PLANNED`.

### Implementation evidence

Completed on 2026-09-09.

Implemented:

- Enabled TypeScript `strict` mode in both projects while preserving existing compiler options.
- Added Vitest 5.0.0, React Testing Library 16.3.3, user-event 14.6.7, jest-dom 7.0.1, jsdom 30.0.1, and required peer `@testing-library/dom` 10.4.1. npm added 77 packages, audited 240 packages, and found no vulnerabilities.
- Extended the existing Vite config with jsdom and `src/test/setup.ts`. Globals remain disabled; setup imports jest-dom for Vitest and registers explicit cleanup with Vitest `afterEach`.
- Added the installed flat Prettier compatibility config after existing ESLint rules, plus minimal Prettier config and ignores.
- Added `test`, `test:watch`, `format`, and `format:check` scripts while preserving existing scripts.
- Added coverage and environment ignore rules with an exception for `.env.example`.
- Updated the lockfile through the scoped install. Every pre-existing locked package retained its prior version.
- Prettier removed only extra blank lines around README code fences; README content was preserved.

Actual verification:

| Check                                 | Result                                                                                                                                                                         |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `npm.cmd ls --depth=0`                | Passed, exit 0; all direct dependencies and required peers resolve.                                                                                                            |
| Effective TypeScript config           | Passed: `strict=true`; the app project includes `src/test/setup.ts`, and the Node project includes `vite.config.ts`.                                                           |
| `npm.cmd run build`                   | Passed, exit 0; TypeScript and the Vite production build completed.                                                                                                            |
| `npm.cmd run lint`                    | Passed, exit 0.                                                                                                                                                                |
| Initial `npm.cmd run format:check`    | Failed, exit 1, on README blank-line formatting; the inspected whitespace-only correction was applied.                                                                         |
| `npm.cmd test`                        | No tests found; Vitest exited 1. This is the accepted T00 empty-suite limitation, not a passing test result. `passWithNoTests` was not added.                                  |
| Initial bare `vitest` watch script    | Reported no tests and exited 1 in this environment, so the script was corrected to explicit `vitest --watch`.                                                                  |
| `npm.cmd run test:watch`              | Entered DEV watch mode and reported no test files without config/import errors; stopped manually. No functional pass is claimed.                                               |
| `npm.cmd run dev -- --host 127.0.0.1` | Vite was ready in 366 ms; the page and transformed `App.tsx` returned HTTP 200; stopped afterward.                                                                             |
| Chrome 152.0.7977.82 smoke check      | Passed in headless Chrome: the existing starter heading and counter rendered. Temporary artifacts are under ignored `node_modules/.tmp`.                                       |
| Git ignore checks                     | Passed for dependencies, dist, coverage, and local environment variants; `.env.example` remains eligible for tracking. No environment contents were read.                      |
| Preservation hashes                   | Passed: setup docs and skills, application source/CSS, `index.html`, and root `tsconfig.json` were unchanged. Expected changes are limited to T00 files and README formatting. |

Final checks after this evidence update: formatting passed (exit 0), lint passed (exit 0), production build passed (exit 0), and direct dependency resolution passed (exit 0). The final test run again found no tests and exited 1. T01 provides the first functional test-runner verification.

## Review handoff

Reviewed on 2026-09-09.

### Reviewed scope and source revision

The repository is on the unborn `master` branch, so no HEAD or commit range exists. The review covered the uncommitted T00 files listed below, including relevant untracked files. There were no deleted paths. Other setup documentation, skills, and application source were inspected for interaction and scope but excluded from the T00 change conclusion.

SHA-256 fingerprints:

| File or scope                                             | SHA-256                                                            |
| --------------------------------------------------------- | ------------------------------------------------------------------ |
| `package.json`                                            | `d386db1477a58acf96b17e30c3412015f678cc2dae0daa11ea13e7a4acaa5bc5` |
| `package-lock.json`                                       | `df854f8bf9257e5b06c491d3d589960b58db4661d7255af703939001e0b33655` |
| `tsconfig.json`                                           | `770b4140bbb581e2dfd9ea9946ffc9c75a1d86ba7d2db5f77c83e37cbdf9d808` |
| `tsconfig.app.json`                                       | `1b246d31e9698193a0876e8c2522c7956253374b6be831ba24f42f5480711749` |
| `tsconfig.node.json`                                      | `8e04d5d603fdaa9eac3e5d5b0f98f0dc1696ee2fae2852470dcd23e42e798d82` |
| `vite.config.ts`                                          | `b8943164402c1d1f1e64663624bf4a5867b795f04f49cdf16ca479cd479012ea` |
| `eslint.config.js`                                        | `96c5e73db15f2207f331cd4024142f616292902f8d1b4fe0e7c02a1c97c40f98` |
| `.prettierrc.json`                                        | `02c3552291b98bde1f75b63c79b2ce0b9cb209b7558a981d1d3c316e4a2336d5` |
| `.prettierignore`                                         | `4e1e5a5d1b03b1673e466c4d3a52aa6245b4baa5a4228e705fa6889b15653f94` |
| `.gitignore`                                              | `016563719abe8b1053206901d4f2ddd3853b3675f7356f7cabefa19fc508da15` |
| `src/test/setup.ts`                                       | `4f64f1e107d3d51e0280ac9e49413199f9b39fe59395eaa049084710d2c88177` |
| `README.md`                                               | `525037c9ee4a1e86a9e6bd95f184e5f00af518e88fd03245efa00889edb12172` |
| `docs/tasks/T00.md` before this review section            | `f1d11387df0d1505a9ed976b2661308aeeeedefff6bc38ec59504e4fdee7d3f1` |
| T00 section of `docs/plan.md` before review status update | `b6a6043b6fffbbaaa89cbea8de954f3a3dc43be0f43355b738e76010c82afb70` |

### Acceptance review

All nine T00 acceptance criteria were checked. Strict mode is effective in both TypeScript projects. The requested test packages and required DOM peer are installed and resolved. Vite selects jsdom, loads the setup file, and keeps test globals explicit; the setup imports the Vitest matcher integration and registers cleanup. ESLint retains its correctness rules with Prettier compatibility last. Prettier configuration and scripts are consistent. Existing dev/build/lint/preview scripts remain, and the new test/watch/format/check scripts are distinct. Manifest and lockfile roots match. Ignore behavior covers the required paths and permits `.env.example`. The empty suite is reported without masking its exit status.

### Independent verification

Passed during review:

- `npm.cmd run format:check`: exit 0.
- `npm.cmd run lint`: exit 0.
- `npm.cmd run build`: exit 0; strict TypeScript and Vite production build completed.
- `npm.cmd ls --depth=0`: exit 0; direct dependencies and peers resolved.
- Node consistency inspection: exit 0; manifest/lock roots, exact scripts, strict configs, Vitest settings, setup imports, and absence of `passWithNoTests` were consistent.
- Git ignore checks: required paths were ignored and `.env.example` was not ignored.
- `npm.cmd run test:watch`: entered Vitest DEV watch mode and reported no configuration or import error; stopped manually.

Observed limitation:

- **Severity:** Informational, non-blocking. **Location:** repository-wide test discovery through `package.json` `test` and `test:watch`; setup in `src/test/setup.ts`. **Concrete failure scenario:** before T01 adds a test, `npm.cmd test` finds no files and exits 1, so the runner, matchers, component rendering, user-event, and cleanup have not been exercised together by a functional test. **Suggested correction:** implement the required formatter tests in T01 and component tests in later UI tasks; do not add `passWithNoTests` or a placeholder T00 test. This limitation is explicitly allowed for T00.

Not run independently during review:

- The development-server HTTP and headless-browser smoke checks were not repeated. Their build-phase results are recorded above, and T00 made no application UI changes.
- The production API was not called; T00 does not implement API behavior, and automated checks must not depend on it.

### Findings and conclusion

No blocking findings remain. The implementation matches T00's accepted scope and project rules. Review status: `REVIEWED`.

## Shipping evidence

The T00 foundation is being recommitted locally under the authorized history rewrite. Shipping status: `COMMITTED`. Record the resulting commit SHA after the rewrite. No remote or push evidence is recorded, so T00 is not `SHIPPED`. The completed review record above remains unchanged.

# T08 - Cart-ready UI layout

- **ID:** T08
- **Implementation:** PLANNED
- **Review:** PENDING
- **Shipping:** NOT_SHIPPED
- **Dependencies:** T07.
- **Index:** [T08 in the task index](../plan.md#t08--cart-ready-ui-layout)
- **Sources:** [Project instructions](../../AGENTS.md), [assignment](../assignment.md), and [technical decisions](../decisions.md).

## Goal

Update the UI layout so it is ready for cart logic without implementing that logic yet: add an Add to Cart action area below the table (button plus the selected item's Order price), remove the Order price currently shown beside `appliedPaperSize`, enlarge and circularize the `appliedPaperSize` badge, and add a (currently inert) shopping cart icon above the table, opposite the paper-size form.

## Inspection and dependency evidence

Inspected on 2026-09-10.

- T07 restructures `App.tsx` into `PaperSizeForm`, `PriceStatusMessage`, `OrderPriceSummary`, and a composing `PricePanel`, giving this task clear seams: the new Add to Cart bar and the relocated Order price join `PricePanel`'s composition, and the cart icon joins the row alongside `PaperSizeForm`.
- The mockup referenced in `docs/assignment.md` shows a paper-size selector, Apply, Price table, Order price, and Cart, but the reference image itself is unavailable in the repository; this task implements the described elements without pixel-level parity to that mockup.
- `selectedCell` and derived `selectedPrice` already exist in `App` state (from T04); this task only needs to read that existing state to enable/disable the new Add to Cart button and to display the same Order price value in its new location — no new state is required for T08 itself.
- `AGENTS.md` prohibits UI component libraries and CSS frameworks; the cart icon must be a native SVG (or equivalent inline markup) styled with the project's existing CSS Modules and design tokens (`src/index.css`), not an icon library import.
- No modal, notification, or cart-icon markup exists anywhere in the codebase yet (confirmed by a repo-wide search for `cart|modal|toast|notification`), so this task introduces new presentational markup with no prior pattern to conform to beyond the existing status/badge styling conventions in `PriceTable.module.css` and the (post-T07) `PricePanel`/`PaperSizeForm` modules.

No dependency capability or unresolved product input blocks implementation. T09 depends on this task's markup (the Add to Cart button and the cart icon) already existing in the DOM.

## Acceptance criteria

1. A new component renders below `PriceTable` (inside or alongside `PricePanel`) containing an "Add to Cart" `<button>` and the Order price of the currently selected cell.
2. The Add to Cart button is disabled (native `disabled` attribute, not just a visual style) when `selectedCell` is `null`, and enabled when a cell is selected; clicking it while enabled has no persistent effect in this task (no state change, notification, or modal — that is T09).
3. The Order price value shown next to the Add to Cart button behaves exactly as the existing Order price did (em dash before selection, derived formatted price after), reusing the existing derivation and `formatPrice`.
4. The Order price display previously shown beside `appliedPaperSize` is removed from that location.
5. The `appliedPaperSize` badge is visually enlarged (font size and padding) and wrapped in a circular background (equal border-radius/width/height, or `border-radius: 50%`), remaining legible for all four paper-size labels (A4, A5, B4, B5).
6. Above the table, in the same row as the paper-size selection form, a shopping cart icon is added on the opposite side of the row from the form (e.g. form on one side, icon on the other, using flex/grid spacing already established in the layout).
7. The cart icon is a real interactive control (a `<button>` with an accessible name such as "View cart" or `aria-label`), but in this task clicking it has no effect yet (no modal opens) — it exists as a layout placeholder for T09.
8. The cart icon and the Add to Cart bar are keyboard-operable and have visible focus styles consistent with existing controls.
9. Layout remains responsive at the existing breakpoint(s) already defined in the CSS Modules (no horizontal overflow, no overlapping controls at narrow widths).
10. No new application state, dependency, global store, or behavior beyond enabling/disabling the button from existing `selectedCell` is introduced.
11. Existing automated tests continue to pass; new/updated tests cover the button's disabled/enabled states tied to selection and the presence/accessible name of the cart icon and the relocated Order price.
12. The final diff is scoped; acceptance criteria and the diff are inspected; tests, formatter, lint, and build pass; a real Chrome or Firefox check confirms the new layout, badge styling, and responsive behavior.

## Scope and non-goals

Scope: new "Add to Cart" bar component (button + Order price), removal of the old Order price location, `appliedPaperSize` badge restyling, and a new cart icon button placed opposite the paper-size form — all wired only to existing selection state.

Do not implement cart state, adding items, a success notification, a cart modal, quantity controls, item removal, total price, or a Checkout button (all T09). Do not change selection, hover, See more, Apply, or API behavior. Do not add a global store, routing, a backend, or any UI/icon library dependency.

## Implementation assumptions

These task-level choices are separate from assignment requirements:

- The cart icon is implemented as an inline SVG inside a native `<button>`, styled via CSS Modules/tokens, to avoid any icon-library dependency.
- "Opposite side of the row" from the paper-size form is implemented with flex layout (`justify-content: space-between` or equivalent) so the form and the cart icon sit at opposite ends of the same header row; exact spacing is a visual-design detail resolved during implementation.
- The Add to Cart bar is a new component (e.g. `AddToCartBar`) placed after `PriceTable` inside `PricePanel`, receiving `selectedCell`/`selectedPrice` as props rather than owning new state.
- The circular `appliedPaperSize` wrapper uses a fixed-aspect (equal width/height) container so it renders as a true circle regardless of label length; if a label would overflow, font-size is reduced rather than the circle becoming an ellipse.

## Relevant files

| File | Planned treatment |
| --- | --- |
| `src/components/PricePanel/PricePanel.tsx` (+ `.module.css`, from T07) | Compose the new Add to Cart bar below `PriceTable`; remove the old inline Order price usage from beside the badge; restyle the `appliedPaperSize` badge. |
| `src/components/AddToCartBar/AddToCartBar.tsx` (+ `.module.css`) | New: Add to Cart button (disabled/enabled by selection) and Order price, reusing `OrderPriceSummary` or its formatting logic. |
| `src/components/CartButton/CartButton.tsx` (+ `.module.css`) | New: cart icon button placed opposite `PaperSizeForm`; inert in this task. |
| `src/components/PaperSizeForm/PaperSizeForm.tsx` (from T07) or a new header row component | Adjust the surrounding row layout so the form and `CartButton` sit at opposite ends. |
| `src/App.tsx` | Pass `selectedCell`/`selectedPrice` down to the new components; no new state owned here for this task. |
| `src/App.test.tsx` (or component-level tests) | Add coverage for button disabled/enabled state, cart icon accessible name, and relocated Order price. |
| `docs/tasks/T08.md` and `docs/plan.md` | Record progress, verification, review disposition, commit, and push evidence independently. |

No API, hook, package, lockfile, test configuration, or decisions change is planned beyond what is already recorded in `docs/decisions.md`.

## Implementation steps

1. Reinspect the working tree (including T07's resulting structure) and set this task's implementation status to `IN_PROGRESS`.
2. Add the `AddToCartBar` component below `PriceTable`, wired to existing `selectedCell`/`selectedPrice`, with the button disabled when nothing is selected.
3. Remove the old Order price markup from beside the `appliedPaperSize` badge.
4. Restyle the `appliedPaperSize` badge: larger size, circular background wrapper.
5. Add the `CartButton` icon component and position it opposite the paper-size form in the header row.
6. Verify keyboard operability and focus styles for both new controls.
7. Add/update tests for disabled/enabled Add to Cart state, cart icon presence and accessible name, and the relocated Order price.
8. Check responsive behavior at the existing narrow-width breakpoint.
9. Inspect the diff for scope, run formatter/lint/tests/build, and verify in a real browser.
10. Mark implementation `VERIFIED` and record review as `DEFERRED` per the consolidated-review policy; no commit or push during planning.

## Verification plan and meaningful cases

| Check | Expected evidence |
| --- | --- |
| Add to Cart disabled with no selection | Button has `disabled` attribute; no click handler effect. |
| Add to Cart enabled after selection | Button becomes enabled and remains associated with the currently selected cell's price. |
| Order price relocation | Old location no longer renders Order price; new bar shows the same derived value/em dash. |
| Badge restyle | Circular wrapper renders correctly for all four paper sizes at desktop and narrow widths. |
| Cart icon | Renders as a focusable, named button; click has no visible effect in this task. |
| Regression | Selection, hover, See more, and Apply behavior are unaffected. |
| Project checks | Formatter, lint, tests, and build pass. |
| Browser QA | Latest Chrome or Firefox confirms layout, badge shape, and responsive behavior. |

## Risks and blockers

Risk of the circular badge clipping longer content or looking inconsistent across paper sizes; risk of flex layout changes affecting existing header spacing; risk of the disabled/enabled wiring drifting from `selectedCell` if T09 later restructures selection handling. No unresolved blocker prevents implementation.

## Progress and verification evidence

Not yet started. This document reflects planning only; implementation evidence will be recorded here once work begins.

## Review status

PENDING. No review has occurred yet.

## Shipping evidence

NOT_SHIPPED. No commit exists for this task yet.
